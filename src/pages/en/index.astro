---
/*
 * MIT License
 *
 * Copyright (c) 2024 Lina (Lexedia) L.
 */

import BaseLayout from '@/layouts/Base'
import avatar from '@/assets/avatar.png'
import { Image } from 'astro:assets'

import Friend from '@/components/Friend'

import friends from '@/data/friends.json'
---

<BaseLayout title="Home" image={avatar}>
  <section class="relative flex flex-col">
    <div class="flex flex-col max-w-full md:max-w-[calc(100%-18rem)]">
      <h1 class="title">Hey you :3</h1>
      <p class="mb-4 sm:mb-8">
        Hello! I'm Lexedia or <kbd class="px-1.5 py-1 text-xs font-semibold bg-accent text-black rounded-lg">/Lex(?:ie?)?/</kbd> for short.<br />
        I'm a developer, mainly focused on backend development, although I also like to do some frontend stuff (&lt;- me when i lie).
        Dart is by far my most favourite language to work with, but I enjoy writing Rust too.
      </p>
    </div>
    <div class="w-full flex justify-center md:absolute md:top-0 md:right-0 md:w-64">
      <Image
        class="rounded-md w-64 md:w-full h-auto object-cover"
        src={avatar}
        alt='My avatar'
      />
    </div>
  </section>

  <section class="mt-8 sm:mt-16">
    <h2 class="title">What do I do?</h2>
    <p class="mb-4 sm:mb-8">
      Currently? It's complicated.<br />
      You can check all my projects <a class="anchor-link" href="/en/~projects/pages/1">here</a> though!
    </p>
  </section>

  <section class="mt-8 sm:mt-16">
    <h2 class="title">Webring</h2>
    <div class="flex flex-wrap gap-4 md:gap-5 pt-5 sm:gap-3">
      {
        friends.map((friend) => (
          <Friend {...friend} />
        ))
      }
    </div>
  </section>
</BaseLayout>
